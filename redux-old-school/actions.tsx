import { ActionTypes } from "./action-types";

export const increaseInCounter = {
  type: ActionTypes.COUNTER_INCREASE,
};

export const decreaseInCounter = {
  type: ActionTypes.COUNTER_DECREASE,
};
